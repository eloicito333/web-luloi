import { version as VERSION } from '@/../package.json';
export const APP_VERSION = VERSION

export const WIDGET_VERSIONS = {
  counterWidget: 'v.0.5.0',
  letterWidget: 'v0.5.0',
  moreContentSoonWidget: 'v.0.5.0',
}